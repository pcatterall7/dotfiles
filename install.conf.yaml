- defaults:
    link:
        relink: true
        create: true

- clean: ['~']

- link:
    ~/.vimrc:
    ~/.vim: vim
    ~/.zshrc:
    ~/.zprofile:
    ~/Library/Preferences/espanso/default.yml: espanso_default.yml
    ~/.config/nvim: 
        path: nvim
        force: true

